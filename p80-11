//p80-11
#include <iostream>
using namespace std;
int main()
{
	int i, j, k, n;
	cin >> n;
	for (i = 0; i <= n; i++)
		for (j = 0; j <= n; j++)
			for (k = 0; k <= n; k++)
				if (n == i * 1 + j * 2 +  k* 5)
					cout << " 1= " << i << " 2= " << j << " 5= " << k << endl;

				

}
