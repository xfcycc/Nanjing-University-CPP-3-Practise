//p80-12
#include <iostream>
using namespace std;
int main()
{
	loop:
	int k = 0, n, p;
	cin >> n;
	p = n;
	while (p != 0)
	{
		k = k * 10 + p % 10;
		p = p / 10;
	}

	if (k == n)
		cout << "yes" << endl;
	else
		cout << "no" << endl;
	goto loop;

				

}
